<h1>Transcripts : </h1>
<%= link_to 'add new', new_transcript_path  %>
<ul>
<% @transcripts.each do |transcript| %>
  <li> <%= link_to "#{transcript.title} - #{transcript.created_at.to_date}", transcript_path(transcript)  %> </li>
<% end %>
</ul>
